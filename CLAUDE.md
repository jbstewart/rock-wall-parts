# CLAUDE.md

Agent guidance for this repo lives in `AGENTS.md` (the shared, vendor-neutral file). It is
imported below so Claude Code picks it up too — keep edits in `AGENTS.md`, not here.

@AGENTS.md
